/**
 * With backsticks we can embed variables into strings. 
 */

let s = "World";

console.log(`Hello, ${s}!`); 
    // Hello World!

console.log(`
    The sum is:
    ${1 + 2}
` );
    // The sum is:
    // 3